<?xml version="1.0" encoding="UTF-8"?>
<!-- Mapper -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="StudentMapper">
<!-- 	 insert id=joinMember 
	<insert id="joinMember" parameterType="com.choa.member.student.StudentDTO">
		insert into member
		values (#{id}, #{pw}, #{name}, #{age}, #{grade}, #{fname}, #{oriname})
	</insert> -->
	<!-- insert id=joinStudent -->
	<insert id="joinStudent" parameterType="com.choa.member.student.StudentDTO">
		insert into student
		values(#{sid}, #{phone}, #{tid})
	</insert>
	
	
</mapper>